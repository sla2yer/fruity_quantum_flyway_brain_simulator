Implemented the manifest/config stimulus integration in [manifests.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/src/flywire_wave/manifests.py#L35), [config.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/src/flywire_wave/config.py#L13), and [milestone_1_experiment_manifest.schema.json](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/schemas/milestone_1_experiment_manifest.schema.json#L59). Manifests now accept an optional normalized `stimulus` block while keeping the existing top-level `stimulus_family` and `stimulus_name`, validation resolves through the canonical stimulus registry, rejects ambiguous nested/top-level references and seed conflicts, and returns discoverable replay metadata: the resolved stimulus spec, contract version, bundle reference, bundle id, and bundle metadata path. Config loading now exposes the same resolved stimulus/bundle metadata through `paths.processed_stimulus_dir`, with bundle helper methods added on [stimulus_registry.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/src/flywire_wave/stimulus_registry.py#L217).

Added regression coverage in [test_manifest_validation.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/tests/test_manifest_validation.py#L15), [test_stimulus_registry.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/tests/test_stimulus_registry.py#L106), and [manifest_stimulus_cases.yaml](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/tests/fixtures/manifest_stimulus_cases.yaml#L1) for healthy resolution, unknown family/preset, invalid family-specific parameters, seed mismatch, and Milestone 1 compatibility.

Verified with `make test`, `make validate-manifest`, and `./.venv/bin/python -m unittest tests.test_manifest_validation tests.test_stimulus_registry tests.test_stimulus_contract -v`.

I could not stage or create the requested commit in this environment because `.git` is read-only here; `git add` fails with `fatal: Unable to create '.git/index.lock': Read-only file system`.