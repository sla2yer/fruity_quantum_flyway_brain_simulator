Implemented the baseline execution layer in [baseline_execution.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/src/flywire_wave/baseline_execution.py). The new canonical API is `resolve_baseline_execution_plan_from_arm_plan`, with `build_baseline_simulator_run_from_arm_plan` and `run_baseline_simulation_from_arm_plan` as convenience entrypoints. It now resolves a baseline arm into one executable run by loading a recorded local canonical input bundle, building a deterministic per-root drive schedule, loading Milestone 7 edge coupling bundles, applying `intact` or `shuffled` topology at the coupling-plan level, and feeding recurrent inputs through provider-owned delay handling instead of the step loop. I also exported the module in [__init__.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/src/flywire_wave/__init__.py).

Added focused regression coverage in [test_baseline_execution.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/tests/test_baseline_execution.py). The fixture exercises deterministic P1 execution from a real recorded retinal bundle plus local coupling assets, verifies delayed recurrent input appears only after the expected number of steps, checks shuffled topology rewires targets deterministically, and fails clearly on missing coupling bundles or timing mismatches.

Verified with `./.venv/bin/python -m unittest tests.test_baseline_execution -v` and `make test`, which passed with 103 tests total.

I attempted the requested stage/commit step, but this sandbox cannot write under `.git`. `git add ...` failed with `fatal: Unable to create '/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/.git/index.lock': Read-only file system`, so I could not stage or create the non-amended commit here.