Implemented the ticket in [registry.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/src/flywire_wave/registry.py#L308) and added regressions in [test_registry.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/tests/test_registry.py#L333) and [test_selection.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/tests/test_selection.py#L116). `materialize_synapse_registry()` now accepts an optional normalized `synapse_df`, `build_registry()` passes its already-loaded synapse table instead of rereading the raw snapshot, and subset-scoped materialization now reuses the current local `synapse_registry.csv` when its provenance shows a compatible `all_rows` build. The written CSV schema, output path, and provenance shape stay the same, including `scope.mode`, `scope.label`, `root_ids`, `root_ids_path`, and source metadata.

Verified with `.venv/bin/python -m unittest tests.test_registry tests.test_selection -v` and it passed. `make test` ran to completion except for one unrelated existing unittest-discovery failure in [tests/test_manifest_validation.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/tests/test_manifest_validation.py) importing `tests.test_simulation_planning`; the module itself passes when run directly with `.venv/bin/python -m unittest tests.test_manifest_validation -v`.