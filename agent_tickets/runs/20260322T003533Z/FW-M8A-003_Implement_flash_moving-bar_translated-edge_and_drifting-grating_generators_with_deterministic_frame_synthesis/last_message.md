Implemented the canonical Milestone 8A generators in [stimulus_generators.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/src/flywire_wave/stimulus_generators.py). The new module synthesizes deterministic frames and direct field samples for `flash`, `moving_bar`, `translated_edge`, `drifting_grating`, and the existing `moving_edge` compatibility alias via the resolved registry spec. It makes the rendering rules explicit for frame timing, centered visual-field coordinates, background luminance, polarity/contrast, motion direction and velocity, aperture handling, grating phase/frequency, and edge ramps, and it records family-specific replay metadata for inspection.

Added fixture-driven regression coverage in [test_stimulus_generators.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/tests/test_stimulus_generators.py) with inputs in [stimulus_generator_cases.yaml](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/tests/fixtures/stimulus_generator_cases.yaml). The tests render one case per implemented family, assert deterministic output, bounds and frame-shape invariants, onset/offset semantics, coordinate conventions, direct sampler behavior, and `moving_edge` compatibility against canonical `translated_edge`.

Verified with `./.venv/bin/python -m unittest tests.test_stimulus_generators -v` and `make test`; the full suite passed (`67` tests).

I could not stage or commit the changes because git writes are blocked in this environment: `fatal: Unable to create '/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/.git/index.lock': Read-only file system`.