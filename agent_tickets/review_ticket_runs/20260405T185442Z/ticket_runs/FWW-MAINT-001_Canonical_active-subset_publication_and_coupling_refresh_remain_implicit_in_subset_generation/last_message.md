Implemented in [selection.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/src/flywire_wave/selection.py#L390). `generate_subsets_from_config()` now has two explicit phases: preset artifact generation and `_publish_canonical_active_subset()` publication of the downstream active subset in [selection.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/src/flywire_wave/selection.py#L503). The publish step now owns `paths.selected_root_ids` updates and any subset-scoped synapse-registry refresh, while preserving the existing artifact files and CLI behavior. The returned `subset_index` summary now includes a top-level `publication` record plus per-preset `publication` metadata indicating whether the active preset was published, whether `selected_root_ids` was updated, and whether subset-scoped coupling artifacts were refreshed.

Coverage was updated in [test_selection.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/tests/test_selection.py#L21) to assert the new publication metadata, the active-preset coupling refresh path, and the no-op alias case on repeated publication.

Verified with `make test` under the repo virtualenv: 330 tests passed.