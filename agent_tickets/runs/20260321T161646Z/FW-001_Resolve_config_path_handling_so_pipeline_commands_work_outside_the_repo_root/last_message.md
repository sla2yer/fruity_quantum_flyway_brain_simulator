Implemented FW-001 by centralizing path normalization in [config.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/src/flywire_wave/config.py#L9). `load_config()` now resolves `config.paths.*` and the pipeline’s default path entries against the repository root, records the absolute config path, and keeps existing repo-root usage working. I also updated [01_select_subset.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/scripts/01_select_subset.py#L17) to use that stored config path instead of recomputing `ROOT / args.config`, and added matching `--config` help text to the four pipeline scripts.

Regression coverage is in [tests/test_config_paths.py](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/tests/test_config_paths.py#L19). It verifies explicit project-root resolution in the loader and runs `build_registry.py`, `01_select_subset.py`, `02_fetch_meshes.py`, and `03_build_wave_assets.py` from a non-repo working directory with an absolute config path. The supported behavior is documented in [README.md](/home/jack/Documents/github/personal/fly_neural_simulation/flywire_wave_repo/README.md#L206).

Verified with `./.venv/bin/python -m unittest tests.test_config_paths -v` and `make test` (`14` tests passed). I left unrelated existing worktree changes untouched.